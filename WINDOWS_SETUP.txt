CYBERTRADE // NEON PROTOCOL — Windows Setup (Miniconda only)
==============================================================
Env name: cybertrade (normal Miniconda, no other tools)

QUICK START FOR WINDOWS 10/11 (Miniconda)
------------------------------------------
1. Install Miniconda (official, normal):
   Official docs: https://docs.anaconda.com/miniconda/install/
   Direct download (Windows 64-bit):
     https://repo.anaconda.com/miniconda/Miniconda3-latest-Windows-x86_64.exe
   Run installer:
     - Install for "Just Me" (recommended, goes to %USERPROFILE%\miniconda3)
     - Keep defaults. You can check "Add Miniconda3 to PATH" if you want conda in normal CMD,
       or leave unchecked and use "Anaconda Prompt" from Start Menu (recommended)

2. Open Anaconda Prompt from Start Menu (search "Anaconda Prompt")
   You should see (base) in prompt and conda --version works.

3. In Anaconda Prompt:
   cd /d C:\Users\YourName\Downloads\CYBERTRADE
   conda env create -f environment.yml

   OR double-click setup_conda.bat in Explorer (it uses Miniconda only)

4. Activate:
   conda activate cybertrade

5. Verify:
   python -m cybertrade doctor
   -> Should show 10/10 checks passed

6. Run:
   Browser HUD (recommended, no extra setup):
     run_web.bat
     OR python -m cybertrade web --port 8899 --auto
     Open http://localhost:8899 in Chrome/Edge

   Desktop HUD:
     run_gui.bat
     OR python run_gui.py

   Tests:
     python -m unittest discover -s tests
     -> 593 tests

DAILY USE (Miniconda)
----------------------
Open Anaconda Prompt:
  conda activate cybertrade
  python -m cybertrade run              (paper trading, safe default)
  python -m cybertrade backtest --bars 200
  python -m cybertrade journal

To exit:
  conda deactivate

To remove env:
  conda env remove -n cybertrade

FILES (Miniconda only)
-----------------------
environment.yml          - conda env definition (name: cybertrade, python 3.11, uses Miniconda)
environment-dev.yml      - dev env with black, ruff, mypy
setup_conda.bat          - double-click to create env (Miniconda only, CMD)
setup_conda.ps1          - PowerShell version (Miniconda only)
setup_conda.sh           - Linux/macOS/Git Bash version (Miniconda only)
activate_cybertrade.bat  - double-click to activate env (Miniconda paths)
activate_cybertrade.ps1  - PowerShell activation (Miniconda)
activate_cybertrade.sh   - bash activation (Miniconda)
run_gui.bat              - launch desktop GUI (Miniconda)
run_web.bat              - launch browser HUD (Miniconda)
CONDA.md                 - full documentation (Miniconda only)
WINDOWS_SETUP.txt        - this file

TROUBLESHOOTING (Miniconda)
----------------------------
- 'conda' not recognized:
  Use Anaconda Prompt from Start Menu, not normal CMD.
  In Anaconda Prompt, conda is already in PATH.

- PowerShell blocks .ps1:
  Run: Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
  Then: .\setup_conda.ps1

- tkinter missing in doctor:
  conda install -n cybertrade tk
  Then: python -m tkinter  (should open test window)

- Port 8899 busy:
  python -m cybertrade web --port 0 --auto
  Or close other app using 8899

- conda env create fails:
  conda clean --all
  Then retry: conda env create -f environment.yml --force

REQUIREMENTS
------------
- Windows 10/11 64-bit
- ~500MB free disk for Miniconda + env
- No admin needed if you install for "Just Me" (to %USERPROFILE%\miniconda3)
- Project is 100% Python stdlib + tk + pytest (no compiled deps, pure Miniconda)

LINKS
-----
Miniconda install docs: https://docs.anaconda.com/miniconda/install/
Miniconda Windows exe: https://repo.anaconda.com/miniconda/Miniconda3-latest-Windows-x86_64.sh
(Use .exe for Windows, not .sh)

SUPPORT
-------
See CONDA.md for detailed Miniconda-only guide.
See README.md for trading system documentation.
